define([
    "reqwest"
], function(
    reqwest
) {

    var StoryPackage = function(config) {

        var path = 'http://localhost:9',
            id = config.id;

        this.load = function() {



        };

    };

    return StoryPackage;

});